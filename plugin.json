{

  "name" : "inContactTest",
  "version": "0.0.7",
  "meterVersionRequired" : "4.2.0-611",
  "unsupportedPlatforms" : [ ],
  "tags": "Windows",
  "description": "Extracts InContact metrics from Windows Performance Counters",
  "icon": "icon.png",
  "command": "powershell -NoLogo -NonInteractive -Command Set-ExecutionPolicy RemoteSigned; .\\get-counters.ps1",
  "metrics": [],
  "paramArray": {
    "itemTitle": [
      "displayName"
    ],
    "schemaTitle": "Counter"
  },
  "paramSchema": [
    {
      "title": "Metric Name",
      "name": "displayName",
      "description": "The 'Name' of the metric in Pulse that will contain data for this perf counter.",
      "type": "string",
      "required": true
    },
    {
      "title": "Metric Short Name",
      "name": "displayNameShort",
      "description": "The 'Short name' of the metric in Pulse that will contain data for this perf counter.",
      "type": "string",
      "required": true
    },
    {
      "title": "Metric Identifier",
      "name": "name",
      "description": "The 'Identifier' of the metric in Pulse that will contain data for this perf counter. You must manually create a metric before entering this definition here.",
      "type": "string",
      "required": true
    },
    {
      "title": "Unit",
      "name": "type",
      "description": "Select",
      "type": { "enum" : [["Count of bytes", "bytecount"], ["Duration", "duration"], ["Number", "number"], ["Percentage", "percentage"]]},
      "default": "number",
      "required": true
    },
    {
      "title": "Counter Path",
      "name": "counter_path",
      "description": "The full windows performance counter path",
      "type": "string",
      "required": true
    },
    {
      "title": "Multiplier",
      "name": "multiplier",
      "description": "An integer value to multiply the counter value by. Default: 1",
      "type": "string",
      "default": 1,
      "required": true
    },
    {
      "title": "Poll Interval",
      "name": "defaultResolutionMS",
      "description": "How often you want to collect this performance counter (in milliseconds). Default: 1000",
      "type": "string",
      "default": 1000,
      "required": true
    }
  ]
}
